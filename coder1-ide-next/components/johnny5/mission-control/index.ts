/**
 * Mission Control Components - Barrel Export
 *
 * Kanban-style task tracker for Johnny5 autonomous work.
 * Provides visibility into all queued, active, and completed tasks.
 *
 * Usage:
 *   import { MissionControlTab, TaskCard, ActivityLog } from '@/components/johnny5/mission-control';
 */

// Main Tab Component
export { default as MissionControlTab } from './MissionControlTab';

// Task Card Component
export { default as TaskCard } from './TaskCard';

// Activity Log Component
export { default as ActivityLog } from './ActivityLog';
