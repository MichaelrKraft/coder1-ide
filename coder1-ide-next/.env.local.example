# ============================================
# CODER1 IDE - UNIFIED SERVER ARCHITECTURE ✨
# ============================================
# SIMPLIFIED: Single unified server handles everything!
# 
# UNIFIED NEXT.JS CUSTOM SERVER (Port 3001):
#    ✅ Next.js UI (the IDE interface)
#    ✅ Terminal PTY (integrated terminal functionality) 
#    ✅ WebSocket via Socket.IO
#    ✅ File operations API
#    ✅ Context Folders API
#    ✅ StatusBar with Brain icon
#    ✅ Session management
#    ✅ Enhanced tmux integration ready
#
# ⚠️ IMPORTANT: Always start with:
#    npm run dev
#
# DO NOT USE: npm run dev:legacy (no terminal support)
#
# ACCESS THE IDE AT: http://localhost:3001/ide
#
# TROUBLESHOOTING:
#   - CSS not loading? You're using legacy mode
#   - Terminal shows "Connection lost"? You're using legacy mode
#   - Solution: Use "npm run dev" (unified mode is now default)
#   - See DEVELOPMENT.md for complete guide
# ============================================

# Unified Server Configuration (Single port for everything!)
NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_UNIFIED_SERVER_URL=http://localhost:3001
PORT=3001

# WebSocket Connection (Same server via Socket.IO)
NEXT_PUBLIC_WEBSOCKET_URL=ws://localhost:3001

# ============================================
# Claude CLI Integration - NO API KEYS NEEDED!
# ============================================
# Install Claude Code CLI from https://claude.ai/code
# The system will auto-detect the CLI location
# Only set this if Claude CLI is in a custom location
CLAUDE_CLI_PATH=

# ============================================
# Claudish - Alternative Models via OpenRouter 💡
# ============================================
# Access Grok, GPT-5, MiniMax, and other models with 50-70% cost savings
# Install: npm install -g claudish
# Get API key from: https://openrouter.ai
#
# Supported Models:
#   - x-ai/grok-code-fast-1 (Cheapest - ~$0.50/M tokens)
#   - openai/gpt-5-codex (Balanced - ~$1.00/M tokens)
#   - minimax/minimax-m2 (Alternative - ~$0.80/M tokens)
#   - qwen/qwen3-vl-235b-a22b-instruct (Multimodal - ~$0.90/M tokens)
#   - zhipu-ai/glm-4.6 (Chinese Market - ~$0.10/M tokens)
#
# Configuration Options:
#   1. UI Configuration (Recommended): Settings → AI → Alternative Models
#      - Enable toggle, enter API key, select model - all in the UI
#   2. Environment Variables (Optional): Set these if you prefer file-based config
OPENROUTER_API_KEY=
USE_CLAUDISH=false
CLAUDISH_MODEL=x-ai/grok-code-fast-1

# Memory persistence database location
DATABASE_URL=sqlite://./data/coder1-memory.db

# ============================================
# Eternal Memory - Semantic Search (Intelligence Layer)
# ============================================
# OpenAI API key for generating conversation embeddings
# This enables semantic search - find similar conversations by meaning, not just keywords
# Get your API key from: https://platform.openai.com/api-keys
# Model: text-embedding-3-small ($0.02 per 1M tokens)
# Cost: ~$9.60/year for 100 users
OPENAI_API_KEY=

# ============================================
# Authentication & OAuth
# ============================================
# Google OAuth
# Get these from https://console.cloud.google.com/
# 1. Create a new project or select existing
# 2. Enable Google+ API
# 3. Create OAuth 2.0 credentials
# 4. Add authorized redirect URI: http://localhost:3001/api/v2/auth/google/callback
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# GitHub OAuth (for Team features)
# Get these from https://github.com/settings/developers
# 1. Create a new OAuth App
# 2. Set homepage URL to http://localhost:3001
# 3. Set callback URL to http://localhost:3001/api/v2/auth/github/callback
NEXT_PUBLIC_GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# ============================================
# Team Knowledge Sync (Supabase)
# ============================================
# Create a free Supabase project at https://supabase.com
# Get keys from Project Settings > API
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-key
TEAM_SYNC_INTERVAL_MS=30000

# JWT Secrets (generate random strings)
JWT_SECRET=your-jwt-secret-key-here
JWT_REFRESH_SECRET=your-jwt-refresh-secret-key-here

# ============================================
# Email (SMTP) - Password Reset 📧
# ============================================
# For Gmail:
#   1. Enable 2-factor authentication on your Google account
#   2. Generate an App Password: https://myaccount.google.com/apppasswords
#   3. Use your email as SMTP_USER and app password as SMTP_PASS
# For other providers, get SMTP settings from your email provider
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM="Coder1 <noreply@coder1.dev>"

# ============================================
# Feature Flags
# ============================================
NEXT_PUBLIC_ENABLE_AI_CONSULTATION=true
NEXT_PUBLIC_ENABLE_AGENT_DASHBOARD=true
NEXT_PUBLIC_ENABLE_TWEAKCC=false
NEXT_PUBLIC_ENABLE_CONTAINERS=false

# Memory Context System (Automatic context from previous sessions)
NEXT_PUBLIC_MEMORY_CONTEXT_ENABLED=true
NEXT_PUBLIC_MEMORY_AUTO_INJECT=true

# ============================================
# 🚀 Progressive Disclosure Architecture (PDA)
# ============================================
# Enables the Skills System for 80-90% token reduction
# 
# How it works:
# - Replaces massive AI prompts with 3-tier loading system
# - Tier 1: Metadata (~100 tokens, always loaded)
# - Tier 2: Instructions (~1-3K tokens, loaded on invocation)
# - Tier 3: References (variable, loaded on-demand)
#
# Benefits:
# - 80-90% token reduction (10,700 → 2,400 for session summaries)
# - 5-6x faster responses
# - $1,250/month cost savings for 100 users
# - Intelligent LRU caching
#
# Set to 'true' to enable (PRODUCTION READY)
ENABLE_SKILLS_SYSTEM=false
#
# Skills directory (default: ./skills)
SKILLS_DIR=./skills

# ============================================
# 🌟 ETERNAL MEMORY - Automatic Session Context
# ============================================
# When enabled, Claude automatically remembers your previous sessions
# No manual copy/paste needed - true persistent memory!
# 
# How it works:
# 1. Your session summaries are automatically saved to /summaries/
# 2. When you type 'claude', the last session is loaded
# 3. Context is injected into Claude's prompt
# 4. Claude "remembers" what you were working on
#
# Set to 'true' to enable (ALPHA FEATURE - Ready for testing!)
ENABLE_ETERNAL_MEMORY=true
#
# Maximum age of session to load (in hours, default: 168 = 7 days)
ETERNAL_MEMORY_MAX_AGE_HOURS=168
#
# Maximum context size to inject (in tokens, default: 3000)
ETERNAL_MEMORY_MAX_CONTEXT_TOKENS=3000

# ============================================
# 🛟 Session Rescue - Crash Recovery System
# ============================================
# Automatically detects unexpected shutdowns and offers to restore your previous session.
# When enabled, shows a recovery modal on startup if the IDE closed unexpectedly.
# 
# Features:
#   - Tracks clean vs unexpected exits
#   - Calculates recovery confidence scores (0-100)
#   - Restores files, terminal history, Claude context, UI layout
#   - Smart filtering: only shows recovery for recent, high-quality checkpoints
# 
# How it works:
#   1. On clean exit: Saves exit marker to localStorage
#   2. On startup: Checks if last exit was unexpected
#   3. If unexpected: Finds most recent recoverable checkpoint
#   4. Shows modal: User chooses to recover or start fresh
# 
# Safe to enable - OFF by default for alpha
ENABLE_SESSION_RESCUE=false
NEXT_PUBLIC_ENABLE_SESSION_RESCUE=false
#
# Maximum age for recoverable sessions (in hours)
SESSION_RESCUE_MAX_AGE_HOURS=24
#
# Minimum recovery confidence score to show prompt (0-100)
SESSION_RESCUE_MIN_SCORE=60

# ============================================
# Claude CLI Puppeteer (TRUE Agent Automation) 🎭
# ============================================
# Enables real AI agent automation using Claude CLI instances
# Set to true to enable cost-free multi-agent parallel execution
ENABLE_CLI_PUPPETEER=false

# Claude CLI binary path (auto-detected if not specified)
CLAUDE_CLI_PATH=

# Agent execution limits
MAX_PARALLEL_AGENTS=5
MAX_WORKFLOW_TIME=600000

# Agent behavior settings  
AGENT_RESPONSE_TIMEOUT=30000
AGENT_SETUP_TIMEOUT=10000
AGENT_IDLE_TIMEOUT=300000

# Work tree configuration for git isolation
ENABLE_WORK_TREES=true
WORK_TREE_BASE_DIR=./agent-workspaces

# Debug and logging
CLI_PUPPETEER_DEBUG=false
CLI_PUPPETEER_LOG_LEVEL=info

# Interactive mode for file system tools (experimental)
# WARNING: Enable only for testing - changes agent behavior significantly
ENABLE_INTERACTIVE_CLI=false

# ============================================
# Development Settings
# ============================================
NODE_ENV=development
NEXT_TELEMETRY_DISABLED=1

# ============================================
# Beta Multi-AI Platform Support 🚀
# ============================================
# Enable Beta route with multi-AI features
NEXT_PUBLIC_ENABLE_BETA_ROUTE=false

# Multi-AI Platform Detection
ENABLE_MULTI_AI_DETECTION=false
AI_PLATFORM_CACHE_DURATION=300000

# Supported AI CLI Platforms (comma-separated)
# Options: claude,openai,github-copilot,aider,continue,codeium,cursor,ollama
ENABLED_AI_PLATFORMS=claude,openai,github-copilot,aider

# Default AI Platform (if multiple are available)
DEFAULT_AI_PLATFORM=claude

# Universal AI Memory Layer
ENABLE_UNIVERSAL_MEMORY=false
MAX_MEMORY_CONTEXT_TOKENS=2000
MEMORY_RETENTION_DAYS=30

# AI Platform API Keys (for platforms that need them)
# Note: Some CLIs use their own auth (e.g., gh auth login for GitHub Copilot)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# ============================================
# GLM API Integration (智谱AI) 🚀
# ============================================
# Get your API key from: https://open.bigmodel.cn/
# GLM provides cost-effective AI models as fallback when Claude hits rate limits
# Models: glm-4-flash ($0.10/M tokens), glm-4-air ($1/M), glm-4-plus ($50/M)

# GLM API Key (REQUIRED for GLM features)
GLM_API_KEY=
NEXT_PUBLIC_GLM_API_KEY=

# GLM Cost Tracking
ENABLE_GLM_COST_DISPLAY=true
GLM_COST_PER_MILLION_TOKENS=0.10

# Rate Limit Fallback Settings
ENABLE_RATE_LIMIT_FALLBACK=true
AUTO_SWITCH_BACK_TO_CLAUDE=true
RATE_LIMIT_COOLDOWN_MINUTES=15

# Context Preservation
MAX_CONTEXT_LINES_FOR_GLM=200
ENABLE_CONTEXT_PRESERVATION=true

# ============================================
# Z.AI GLM 4.6 Backend (Overflow Capacity) 🔄
# ============================================
# For Claude Code Pro users who hit rate limits frequently
# Get API key from: https://z.ai (智谱AI reseller)
# Cost: $0.10/M tokens (10x cheaper than Claude)
# Quality: Comparable to Claude Sonnet 4.5

# Enable GLM backend (set to true when configured)
USE_GLM_BACKEND=false

# Z.AI API Credentials
ZAI_API_KEY=
ZAI_BASE_URL=https://api.z.ai/api/anthropic

# ⚙️ How to Use:
# 1. Sign up at https://z.ai and get API key
# 2. Add key above and set USE_GLM_BACKEND=true
# 3. Restart IDE: npm run dev
# 4. Open Terminal Settings → Select "GLM 4.6"
# 5. Use Claude Code normally - runs on GLM at $0.10/M

# ============================================
# GEMINI API (Google) 🚀 NO PASSPORT REQUIRED!
# ============================================
# Google Gemini provides ultra-cheap AI models - RECOMMENDED ALTERNATIVE to GLM
# NO PASSPORT/ID verification required - just Google account!
# Get API key: https://ai.google.dev/ (FREE tier available!)
# Model: gemini-2.5-flash-lite ($0.10/M input, $0.40/M output)

# Gemini API Key (RECOMMENDED - No passport needed!)
GEMINI_API_KEY=
NEXT_PUBLIC_GEMINI_API_KEY=

# Gemini Cost Tracking
ENABLE_GEMINI_COST_DISPLAY=true
GEMINI_COST_PER_MILLION_TOKENS_FLASH_LITE_INPUT=0.10
GEMINI_COST_PER_MILLION_TOKENS_FLASH_LITE_OUTPUT=0.40

# Local AI Support (Ollama)
OLLAMA_HOST=http://localhost:11434
OLLAMA_DEFAULT_MODEL=codellama

# Smart Context Injection
ENABLE_SMART_CONTEXT=true
CONTEXT_RELEVANCE_THRESHOLD=0.7
MAX_CONTEXT_CHUNKS=3

# AI Arbitrage Settings (route to cheapest capable model)
ENABLE_AI_ARBITRAGE=false
COST_OPTIMIZATION_LEVEL=balanced

# Beta Features Analytics
ENABLE_BETA_ANALYTICS=false
BETA_ANALYTICS_ENDPOINT=

# ============================================
# Stripe Payment Integration 💳
# ============================================
# For Pro tier subscriptions ($29/month)
# Get your API keys from: https://dashboard.stripe.com/apikeys
# Create a product: https://dashboard.stripe.com/products
#
# 1. Create a "Coder1 Pro" product with $29/month recurring price
# 2. Copy the Price ID (starts with price_...)
# 3. Set up webhook endpoint: https://yourdomain.com/api/stripe/webhook

# Stripe API Keys (REQUIRED for payments)
STRIPE_SECRET_KEY=sk_live_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_...

# Stripe Price ID for Pro plan (REQUIRED)
STRIPE_PRO_PRICE_ID=price_...

# Stripe Webhook Secret (REQUIRED for production)
# Get this from: https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET=whsec_...

# ============================================
# Go High Level CRM Integration 🚀
# ============================================
# GHL provides professional CRM, email automation, and customer tracking
# Get your API credentials from your GHL account settings
# Documentation: https://developers.gohighlevel.com/

# GHL API Authentication
GHL_API_KEY=
GHL_CLIENT_ID=
GHL_CLIENT_SECRET=
GHL_LOCATION_ID=

# GHL Webhook Configuration
# Set this in GHL webhook settings: http://localhost:3001/api/webhooks/ghl
GHL_WEBHOOK_SECRET=

# GHL Environment (production or sandbox)
GHL_ENVIRONMENT=production

# Feature Flags for GHL Integration
ENABLE_GHL_INTEGRATION=false
ENABLE_GHL_AUTO_SYNC=true
ENABLE_GHL_WEBHOOKS=true
ENABLE_GHL_EMAIL_CAMPAIGNS=true

# GHL Sync Settings
GHL_SYNC_INTERVAL_MS=300000
GHL_BATCH_SIZE=50
GHL_RETRY_ATTEMPTS=3

# GHL Custom Field IDs (map these to your GHL custom fields)
# These will be created if they don't exist
GHL_FIELD_USER_ID=
GHL_FIELD_SUBSCRIPTION_TIER=
GHL_FIELD_LAST_PROJECT=
GHL_FIELD_TOTAL_PROJECTS=
GHL_FIELD_AI_USAGE=
GHL_FIELD_LAST_ACTIVE=

# ============================================
# 🤖 J5 Integration (Autonomous AI Agent)
# ============================================
# Connect Johnny5 Dashboard to J5 daemon for 24/7 autonomous capabilities
# J5 provides: session persistence, skills execution, messaging channels
#
# ⚠️ SECURITY WARNING: Do NOT use default port 18789 (scannable by Shodan)
# Use a CUSTOM port (e.g., 21543, 19876, 28956) for security
#
# Network Setup:
# - J5 runs on a SEPARATE machine (not localhost)
# - Both machines must be on same local network
# - Never expose J5 to the internet

# Enable J5 integration (set to true when J5 is ready)
J5_ENABLED=false

# J5 Gateway URL (replace with your J5 laptop's IP and CUSTOM port)
# Example: ws://192.168.1.100:21543
J5_GATEWAY_URL=

# J5 Authentication Token (REQUIRED for secure connections)
# Get this token from ManusLive: Settings > Gateway > Auth Token
# The token is used during the WebSocket handshake to authenticate Coder1
J5_AUTH_TOKEN=

# Connection settings
J5_RECONNECT_INTERVAL=5000
J5_MAX_RETRIES=10
J5_CONNECTION_TIMEOUT=30000

# Fallback behavior when J5 is unavailable
# true = fall back to direct Claude API
# false = show error when J5 disconnected
J5_FALLBACK_TO_DIRECT=true

# ManusLive Daemon Auto-Start (Alpha Feature)
# Coder1 will auto-start ManusLive daemon when server starts
# This enables full Johnny5 autonomy out of the box
#
# Path to ManusLive installation (default: ~/manuslive/manuslive)
MANUSLIVE_PATH=
#
# ManusLive gateway port (default: 55413)
MANUSLIVE_PORT=55413

# ============================================
# 🔧 Johnny5 Bridge MCP Tool Access
# ============================================
# When enabled, Johnny5 Bridge mode adds --permission-mode bypassPermissions
# to Claude Code CLI commands, allowing MCP tools (youtube, transcript-api, etc.)
# to execute without interactive permission prompts.
# Also injects --add-dir for project file access.
#
# Default: false (safe — no behavior change)
# Set to true after connecting bridge to enable autonomous MCP tool use
JOHNNY5_BRIDGE_MCP_ENABLED=false
# ============================================================
# FlowTrace — Developer Session Memory
# ============================================================
# Requires Screenpipe to be installed separately: https://screenpipe.com

# Enable FlowTrace capture (default: off — explicit opt-in required)
FLOWTRACE_ENABLED=false

# Your Gemini API key — used for multimodal embeddings (gemini-embedding-2-preview)
# Get a free key at: https://aistudio.google.com/apikey
# Set GEMINI_API_KEY above (under GEMINI API section) — no separate key needed here.

# Pinecone API key (optional — leave blank for local sqlite-vec storage)
# Get free key at: https://pinecone.io
PINECONE_API_KEY=

# Pinecone index name (auto-created on first run)
FLOWTRACE_PINECONE_INDEX=flowtrace-dev-sessions

# Capture behavior (seconds)
FLOWTRACE_MIN_INTERVAL_SECS=30
FLOWTRACE_MAX_INTERVAL_SECS=300

# Retention: auto-delete vectors older than N days (0 = no auto-delete)
FLOWTRACE_RETENTION_DAYS=90

# ============================================================
# AI OS: Knowledge Base (Vault)
# ============================================================

# Enable the built-in Obsidian-like notes system
NEXT_PUBLIC_VAULT_ENABLED=false

# Path to your notes vault (default: ~/.coder1/knowledge)
# Point this at your existing Obsidian vault to use it instead
NEXT_PUBLIC_VAULT_PATH=~/.coder1/knowledge

# ============================================================
# AI OS: Ambient Screen Activity
# ============================================================

# Enable ambient screen context injection into sessions
NEXT_PUBLIC_AMBIENT_ENABLED=false

# ============================================================
# AI OS: Johnny5 Miracles
# ============================================================

# Enable Johnny5 autonomous agent features (Dream Weaver, Idea Collision, etc.)
NEXT_PUBLIC_JOHNNY5_MIRACLES_ENABLED=false
